# AI Resume Screening - requirements.txt
# Install with: pip install -r requirements.txt

# Web framework
flask>=3.0.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.1.0

# Security
werkzeug>=3.0.0

# Environment
python-dotenv>=1.0.0

# AI / LLM
groq>=0.9.0

# ML models
# The .pkl files are version-sensitive. Avoid major version upgrades unless
# the models are retrained and re-exported.
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
joblib>=1.3.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Resume file parsing
pdfplumber>=0.10.0
python-docx>=1.1.0

# PDF export
reportlab>=4.0.0

# Duplicate detection
# Installs sentence-transformers + torch. The embedding model downloads on
# first use if it is not already cached.
sentence-transformers>=2.7.0

# Optional production dependencies:
# psycopg2-binary>=2.9.0
# sendgrid>=6.11.0
# boto3>=1.34.0
# celery>=5.3.0
# redis>=5.0.0
# gunicorn>=21.0.0
